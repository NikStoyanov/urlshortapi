sudo: false
language: python
python:
  - "3.6"
install: pip install tox-travis
script:
  - tox
deploy:
  provider: script
  script: pip install zappa && zappa update production
  skip_cleanup: true
  on:
    branch: master
